import React from 'react'
import ProductionCameraManager from './ProductionCameraManager'

// Use the production camera manager as the main camera manager
const CameraManager = () => {
  return <ProductionCameraManager />
}

export default CameraManager